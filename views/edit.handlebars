<form class="editPage" action="/restaurants/{{ restaurant.id }}?_method=PUT" method="POST">

    <p class="form-group" style="font-weight:bold;font-size: 18px; text-align:center">歡迎編輯餐廳資訊！<br />請注意有*註記者為必填欄位喔！</p>
    <div class="form-group">
      <label for="restaurantName">餐廳名稱 *</label>
      <input type="text" class="form-control" id="restaurantName" name="name" value="{{restaurant.name}}" placeholder="請輸入餐廳名稱" required
        oninvalid="alert('請輸入餐廳名稱！');">
    </div>
    <div class="form-group">
      <label for="restaurantNameEn">餐廳英文名稱</label>
      <input type="text" class="form-control" id="restaurantNameEn" name="name_en" value="{{restaurant.name_en}}" placeholder="English name here">
    </div>
    <div class="form-group">
      <label for="restaurantCategory">餐廳分類 *</label>
      <select class="form-control" id="restaurantCategory" name="category" required>
        <option {{#is restaurant.category '中東料理'}}selected{{/is}}>中東料理</option>
        <option {{#is restaurant.category '義式餐廳'}}selected{{/is}}>義式餐廳</option>
        <option {{#is restaurant.category '日本料理'}}selected{{/is}}>日本料理</option>
        <option {{#is restaurant.category '美式'}}selected{{/is}}>美式</option>
        <option {{#is restaurant.category '酒吧'}}selected{{/is}}>酒吧</option>
        <option {{#is restaurant.category '咖啡'}}selected{{/is}}>咖啡</option>
      </select>
    </div>
    <div class="form-group">
      <label for="restaurantImage">圖片URL</label>
      <input type="text" class="form-control" id="restaurantImage" name="image" value="{{restaurant.image}}" placeholder="">
    </div>
    <div class="form-group">
      <label for="restaurantLocation">餐廳地址</label>
      <input type="text" class="form-control" id="restaurantLocation" name="location" value="{{restaurant.location}}" placeholder="">
    </div>
    <div class="form-group">
      <label for="restaurantPhone">電話號碼</label>
      <input type="text" class="form-control" id="restaurantPhone" name="phone" value="{{restaurant.phone}}" placeholder="02-12345678">
    </div>
    <div class="form-group">
      <label for="restaurantMap">Google地圖連結</label>
      <input type="text" class="form-control" id="restaurantMap" name="google_map" value="{{restaurant.google_map}}" placeholder="">
    </div>
    <div class="form-group">
      <label for="restaurantRating">餐廳評分</label>
      <input type="number" class="form-control" id="restaurantRating" name="rating" value="{{restaurant.rating}}" placeholder="" min="1.0"
        max="5.0" step="0.1">
    </div>
    <div class="form-group">
      <label for="restaurantDescription">餐廳介紹</label>
      <textarea class="form-control" id="restaurantDescription" name="description" rows="7"
        placeholder="為餐廳加點介紹文吧！">{{restaurant.description}}</textarea>
    </div>
    <div class="form-group" style="display:flex;justify-content: center; margin: 10px;">
      <button type="submit" class="btn btn-primary" style="margin: 10px">儲存</button>
      <a class="btn btn-primary" href="/" role="button" style="margin: 10px">返回首頁</a>
    </div>
</form>